spring.application.name=bookmyshow-backend

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/bookmyshow?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=#Lekhya@123#
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server Configuration
server.port=8090

# Initialize database with data.sql
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# Email Configuration
# To enable actual email sending, uncomment and configure with real SMTP settings:
 spring.mail.host=smtp.gmail.com
 spring.mail.port=587
 spring.mail.username=lekhya111ponnamanda@gmail.com
 spring.mail.password=flxa wydn puqh nfgp
 spring.mail.properties.mail.smtp.auth=true
 spring.mail.properties.mail.smtp.starttls.enable=true
 spring.mail.properties.mail.smtp.starttls.required=true
# For Gmail, you need to use an App Password, not your regular password

# SMS Configuration (for production, use Twilio or similar service)
# sms.api.key=your-sms-api-key
# sms.api.url=https://api.sms-provider.com

# Phone Call Configuration
phone.call.enabled=true
# phone.call.api.key=your-phone-api-key
# phone.call.api.url=https://api.phone-provider.com
# For production, use services like Twilio Voice API or AWS Amazon Connect


